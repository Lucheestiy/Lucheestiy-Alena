services:
  web:
    image: nginx:1.25-alpine
    container_name: alena-landing
    restart: unless-stopped
    ports:
      - "${ALENA_HOST_PORT:-8099}:80"
    volumes:
      - ./html:/usr/share/nginx/html:ro
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
